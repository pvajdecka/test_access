name: raw-ssh
on:
  workflow_dispatch:          # run it manually from the “Actions” tab

jobs:
  run-ssh:
    runs-on: ubuntu-latest    # GitHub-hosted runner
    steps:
      - name: Run the SSH command
        run: |
          ssh ubuntu@18.215.219.106

